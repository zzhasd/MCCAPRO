# 多机器人“分区—各向异性铺砖—避障巡回”算法升级与实验报告

## 1. 结论先行

本次升级将原文件拆成可单独评价的三个阶段，并实现为 `hybrid_mcpp.py`。升级算法暂称 **E²TR-MCPP**（Epsilon-balanced partition, Exact-neighborhood Tiling, Roadmap-TSP routing）。在 5 个地图尺度（20、50、100、150、200）、3 个随机种子、5 台机器人、约 10% 随机障碍的 15 组配对实验中：

- 分区面积 CV：15/15 组降低，配对平均降低 **94.06%**；
- 总铺砖数：12/15 组降低，配对平均降低 **1.93%**，中位降低 **2.58%**；
- 闭合路线总长度：15/15 组降低，配对平均降低 **32.13%**；
- 最大 200×200 实例：升级算法最慢 **13.96 s**，没有分钟级或半小时级求解；
- 15/15 组均通过“分区连通、砖无重叠、无漏铺、无越障碍”的结构校验。

需要诚实说明：20×20 的 3 个离散小图中，总砖数有 2 个退化、1 个持平。原因不是局部铺砖器没有求到最优，而是强均衡分区改变了区域形状；这表明面积均衡与可铺性之间存在真实 Pareto 冲突。因此最终代码同时报告面积、砖数、路径三种 CV，不再用单一面积 CV 代表全部工作负载。

## 2. 原算法审计

原文件的优点是结构清楚、能覆盖 20–200 网格、砖型定义明确。但三阶段各有一个核心缺口：

1. **分区**：多源 FIFO BFS 的冲突由队列先后顺序决定；随后 Lloyd 式质心更新只改善几何位置，没有容量约束。因此它是普通地理 Voronoi 的离散近似，不是负载均衡分区。
2. **铺砖**：只对四种扫描方向运行“先大后小”贪心并取最小值。扫描一旦放错大砖，后续没有交换、撤销或局部精确重排。
3. **路径**：代码只构造相邻砖图的 MST 边，没有生成访问所有砖中心的实际巡回路线。若按常见“绕树”闭合走法计算，需要重复每条树边，总长度为约 `2 × MST`。

此外，原文件依赖 `networkx`；本实验环境没有该包，原文件会在导入阶段失败。升级实现仅依赖 NumPy、SciPy 和可选的 Matplotlib。

## 3. 三种 CV 的定义

对机器人 (i=1,dots,K)，令 (A_i) 为分区内可通行小网格数，(B_i) 为铺砖数，(L_i) 为闭合路线长度。统一使用：

\[
CV(x)=\frac{\operatorname{std}(x_1,\ldots,x_K)}
{\operatorname{mean}(x_1,\ldots,x_K)}.
\]

- `partition_cv = CV(A)`：分区小网格面积平衡；障碍不计入面积。
- `tile_cv = CV(B)`：各机器人需要处理的砖块数量平衡。
- `path_cv = CV(L)`：各机器人实际巡回距离平衡。

这三者不能互相替代。各向异性砖使得同面积区域可能产生不同砖数；不同障碍拓扑又会使同砖数区域产生不同路径长度。

## 4. 升级算法

### 4.1 ε-均衡的连通测地幂分区

在自由网格图上，先从每个机器人种子做无障碍最短路，得到测地距离 (d_G(s_i,v))，再按加性权重分配：

\[
r(v)=\arg\min_i\{d_G(s_i,v)+\lambda_i\}.
\]

对偶权重按区域容量误差迭代更新。加性权重测地 Voronoi 单元具有从单元格沿最短路回到本种子的闭包性质，因此非空分区保持连通。算法不是一味追求 `CV_area=0`：当面积 CV 进入 ε 带后，在候选中优先选择分区边界较短者，避免为移动最后几个格子制造锯齿边界并破坏 2×2、4×4 对齐。

20×20 等小图另做尺度自适应联合选择：对少量候选分区直接求各区域的精确铺砖数，再在面积平衡与总砖数之间选择候选。大图不做全局联合 MIP，从而保持扩展性。

这一设计借鉴了 DARP 的等面积连通分区目标和 SCoPP 的可扩展负载均衡思想，但使用的是加权测地幂图与下游可铺性耦合。[DARP 原论文](https://link.springer.com/article/10.1007/s10846-016-0461-x)、[DARP 官方实现](https://github.com/athakapo/DARP)、[SCoPP](https://arxiv.org/abs/2103.14709)。

### 4.2 多启动贪心 + 限时局部精确铺砖

对所有合法放置 (p) 建立二元变量 (z_p)。局部精确模型为：

\[
\min \sum_p z_p,
\qquad
\sum_{p:c\in p}z_p=1,\quad \forall c.
\]

等式约束同时保证无漏铺和无重叠。算法先运行四向扫描和随机多启动贪心；小区域直接全局 MILP，大区域只在 12×12 问题窗口内做限时精确重排，每个窗口最多约 0.12 s，并设置整个铺砖阶段的时间预算。这相当于“快速可行解 + 大邻域精确搜索”，避免把 200×200 的全部合法砖位一次性交给 MIP。

相关理论背景是矩形覆盖/分解的整数规划与局部搜索；一般矩形覆盖问题具有组合困难性，全局精确模型适合小规模，分解或局部精确化更适合大规模。[Rectangle Covers Revisited Computationally](https://www.or.rwth-aachen.de/files/research/publications/rcover-jea.pdf)、[Rectangle Blanket 的整数规划与分支定价](https://arxiv.org/abs/1910.01193)、[集合覆盖局部搜索](https://arxiv.org/abs/2211.04444)。

### 4.3 砖中心几何路网 + 最短路度量闭包 + 2-opt

每块砖的几何中心作为必须访问的节点。若两块砖共享网格边，则在共享边上选择使“中心—门点—中心”折线最短的门点，并以该折线作为安全路网边。中心到门点的两段分别位于各自无障碍矩形内部，因此即使两砖只共享部分边界也不会穿越障碍。对该稀疏几何路网计算全对最短路，得到障碍感知距离矩阵 (D)。

在 (D) 上用最近邻构造闭合 TSP 路线，再做 2-opt 改善；最后通过最短路前驱矩阵把每条度量闭包边展开成实际中心序列，输出可执行闭合折线，而不只是访问顺序。最短路采用 Dijkstra；TSP 局部改进来自 Lin–Kernighan 家族中最实用的低阶交换思想。[Dijkstra 原论文](https://link.springer.com/article/10.1007/BF01386390)、[Lin–Kernighan 原论文](https://dl.acm.org/doi/abs/10.1287/opre.21.2.498)、[Helsgaun 的高效实现研究](https://www.sciencedirect.com/science/article/abs/pii/S0377221799002842)。

与直接在欧氏完全图上连线不同，这里任意输出线段都来自相邻自由砖；与绕 MST 不同，TSP 路线不必重复所有树边。

## 5. 实验设置

- 网格边长：20、50、100、150、200；
- 机器人：5；障碍率：目标 10%；
- 随机种子：42、2024、8191；
- 若随机障碍产生没有任何种子的孤立自由口袋，则将该口袋归为障碍。原 BFS 也无法覆盖这些口袋，因此这是完整覆盖实验所需的连通性预处理；
- 每张地图上，基线和升级算法使用完全相同的障碍图；
- 基线路径长度按原 MST 的标准闭合绕树长度 `2 × MST edge sum` 计算；
- 环境：Python 3.12.13、NumPy 2.3.5、SciPy 1.17.0，AMD EPYC 9V74 容器 CPU；
- 表内数值为 3 个种子的均值，时间为单进程墙钟时间。

## 6. 结果

| N | 面积 CV：原→新 | 砖数 CV：原→新 | 路径 CV：原→新 | 总砖数：原→新 | 总路径：原→新 | 时间(s)：原→新 |
|---:|---:|---:|---:|---:|---:|---:|
| 20 | 0.1749→0.0146 | 0.2220→0.1610 | 0.1869→0.2064 | 93.3→98.0 | 329.2→267.8 | 0.06→1.03 |
| 50 | 0.1351→0.0065 | 0.1697→0.0678 | 0.1504→0.0362 | 530.0→497.3 | 1961.9→1251.2 | 0.41→1.69 |
| 100 | 0.1266→0.0066 | 0.1381→0.0383 | 0.1262→0.0361 | 2048.0→1947.0 | 7571.7→4852.0 | 1.78→4.15 |
| 150 | 0.1162→0.0071 | 0.1153→0.0255 | 0.1200→0.0235 | 4596.0→4469.7 | 16931.0→10843.1 | 4.17→8.21 |
| 200 | 0.1203→0.0072 | 0.1204→0.0204 | 0.1217→0.0215 | 8118.0→8055.3 | 29814.7→19673.7 | 7.04→13.65 |

跨全部 15 个样本直接汇总均值：

- `CV_area`：0.1346 → 0.0084，降低约 **93.8%**；
- `CV_tile`：0.1531 → 0.0626，降低约 **59.1%**；
- `CV_path`：0.1410 → 0.0647，降低约 **54.1%**。

单侧配对 Wilcoxon 检验结果：面积 CV `p=3.05e-5`，砖数 CV `p=6.10e-5`，路径 CV `p=0.00513`，总砖数 `p=0.000944`，总路径 `p=3.05e-5`。样本只有 15 个，这些结果可作为工程验证，不应冒充大样本统计结论。

### 6.1 尺度效应

- 50–200 的 12 个样本中，总砖数全部下降；20×20 的离散边界效应最强。
- 路径缩短在 50–200 上稳定约 34%–36%。
- 新算法时间从 20×20 的约 1 s 增长到 200×200 的约 14 s，仍处于交互式工程可用范围。
- 200×200 时总砖数降幅约 0.77%，说明 10% 独立随机点障碍会打碎大量 4×4 候选，限制了任何铺砖算法能获得的绝对收益。对块状障碍、走廊和房间地图还应单独实验。

## 7. 创新度判断

可以合理表述、且代码已经实现的创新点是：

1. **分区与铺砖耦合**：在面积 CV 的 ε 约束下优化边界可铺性，而非把分区和平铺完全串行割裂。
2. **尺度自适应精确化**：小地图全局精确、大地图局部限时精确，统一处理 20–200 网格。
3. **中心级避障 TSP**：在由砖几何本身诱导的安全路网上建立最短路度量闭包，再优化巡回次序并展开实际路径。
4. **三负载指标**：同时评价面积、砖数、路径三种 CV，揭示面积均衡与真实执行负载不等价。

不建议现在就写“首次提出”或“全局最优”。要做论文级新颖性声明，还需系统检索 Web of Science/Scopus、学位论文和专利，并与 DARP、SCoPP、MSTC*/TMSTC*、矩形分解和 obstacle-aware TSP 的最新变体逐项对照。

## 8. 仍需补强的实验

1. 每个尺度至少 30–50 个种子，并报告置信区间；
2. 增加障碍形态：独立点、矩形块、房间走廊、窄通道、真实栅格地图；
3. 机器人数量从 3 扩展到 21，评估三种 CV 和最大单机器人完成时间 `max(L_i)`；
4. 做消融：仅新分区、仅局部 MILP、仅 TSP，以及不同 ε、窗口、时间预算；
5. 与 DARP/SCoPP/TMSTC* 的公开实现做同图比较；
6. 若机器人有非零半径，先对障碍做配置空间膨胀；若多机器人会同时运动，还需增加时空冲突消解，本版本只保证静态障碍安全。

## 9. 复现实验

```bash
python benchmark_mcpp.py
```

原始逐例数据、聚合结果和对比图分别为 `results/benchmark_raw.csv`、`results/benchmark_summary.json`、`results/benchmark_comparison.png`。

生成 20、50、100、150、200 五种边长的优化前后分区、砖块轮廓与路径图：

```bash
python visualize_comparisons.py
```

脚本会在 `visualizations/` 下生成五张双栏图和一张 `partition_tile_path_comparison_all_sizes.png` 总览图。黑色为障碍；灰黑细矩形为每块各向异性砖的真实轮廓；五种区域底色和同色折线分别对应五台机器人；星号是各机器人的分区种子。优化前折线为实际 MST 闭合绕树，优化后折线通过砖共享边界门点展开，几何上保证不穿静态障碍。

第一次运行会把障碍矩阵、优化前后分区、所有砖、展开路径、种子和指标压缩保存到 `visualization_cache/*.npz`。以后修改配色、线宽、图例或版式时，普通运行只读取缓存，不会再次运行分区、MILP 和路径优化。若算法本身发生变化并需要刷新实验数据，使用：

```bash
python visualize_comparisons.py --force-recompute
```
